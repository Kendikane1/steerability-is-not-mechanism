# Phase roadmap

Status, 2026-09-22: Phase 1 milestones 1–2 have local engineering evidence: pinned input/runtime
specification and one verified synthetic Qwen scoring pass on MPS (P1-013). Follow
`RESEARCH_WORKFLOW.md` and `RESEARCH_LOG.md`. Milestone3 capture/identity and within-process
repeatability passed for one prompt/site (P1-014); zero-dose and paired/reverse geometry passed
for two synthetic directions (P1-015). Milestone4 short local shard/SIGKILL/resume checks passed
with exact cross-process array equality (P1-016). Milestone5 consolidated review is complete
(P1-017; `PHASE1_ENGINEERING_REVIEW.md`),
with an explicit historical console-checksum amendment and fresh verification.
The broader Phase1 exit gate is not yet met: multi-hour reliability, safe memory headroom and
scientific choices remain open. The sustained local synthetic limits are approved (`LOCAL_SUSTAINED_TEST_PLAN.md`).
P1-019 refused warning pressure; the user later explicitly permitted warnings. P1-020 implements
the scoped rehearsal/monitor but stopped during loading at the unchanged swap-growth cap, before
any forward. See `SUSTAINED_REHEARSAL_RESULT.md`. Diagnose host demand before a new attempt;
rehearsal acceptance and multi-hour execution remain incomplete.
P1-022 transferred the pinned engineering model to Windows: single, mechanics and short resume
supervisors and the subsequent artifact audit passed (`REMOTE_ENGINEERING_RESULT.md`).
P1-023 rehearsal-01 stopped on a Windows progress-file error; its fix is verified and a manual
rehearsal-02 stopped during loading at the host RAM reserve with0 forwards. Rehearsal acceptance
and multi-hour execution remain pending. No scientific execution has begun.

| Phase | Scope | Exit condition |
|---|---|---|
| 0 — initialization | Specifications, typed configs, modular skeleton, synthetic fixtures/tests. No weights or scientific data. | Full local verification and synthetic smoke pass. |
| 1 — engineering smoke | Freeze 0.6B revision/token protocol; implement adapter/hooks; test deterministic capture, replacement, sharding, and resume locally. | Engineering gate passes; no scientific inference. |
| 2 — two-week scientific pilot | Build independent pilot-only direction material and 20 objective items; verify the phenotype and gates on scientifically relevant compute. | Predeclared go/no-go or documented pivot; pilot never enters final estimate. |
| 3 — development | Audit/freeze direction sets and objective generator; screen neutral items; select one layer and intervention/control rules using development only. | All scientific/analytic decisions resolved without locked-test inspection. |
| 4 — freeze/preregister | Finalize hypotheses, exclusions, splits, statistics, controls, exact revisions and hashes; create immutable locked manifest. | Preregistration frozen and explicit execution acknowledgement recorded. |
| 5 — locked execution | Run all main conditions and controls in deterministic resumable shards without partial-result inspection; then execute frozen analysis. | Immutable result bundle, intervals, diagnostics, deviations, and bounded conclusion. |

Colab replication or a second model family begins only after the one-model core is complete and
cannot alter the locked primary analysis.

P1-023 update: user reports manual attempt03 passed the rehearsal (193 forwards), but projected
long-run full-array storage184.022 GiB exceeds the4 GiB cap. Audit the saved evidence and validate
a prospective lossless storage revision before multi-hour execution. No scientific inference.

P1-024: independent rehearsal audit passed; the offline lossless archive reconstructs every job
exactly and reduces85.10 MiB to3.86 MiB. Conditional long-run storage estimate1.44 GiB fits the
cap, but a durable bounded-memory live writer and its short rehearsal remain prerequisites.
See `REHEARSAL_STORAGE_REVIEW.md`; no multi-hour or scientific execution has begun.

### P1-025 update — live storage and manual short test

Live lossless writer and abrupt-process-exit/corruption tests implemented. Replaying saved
engineering artifacts reconstructed96 records exactly (7,774,208 bytes across two independent
arm stores). New compact short rehearsal is the next model-backed gate; multi-hour and scientific
execution remain unestablished. See `MANUAL_COMPACT_REHEARSAL.md` and its prospective protocol.

### P1-026 update — compact audit passed, timing feasibility next

All192 saved across-arm arrays match exactly; recovery/resource checks pass. Conditional long
storage fits, but short-run elapsed-time extrapolation exceeds the3-hour arm cap. Profile and
reduce measured overhead before recalibration and long-run launcher preparation. No long run
has started. See `COMPACT_REHEARSAL_RESULT.md`.
